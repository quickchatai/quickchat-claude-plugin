---
description: Find and triage Quickchat conversations that need a human
argument-hint: "[agent name]"
---

Triage the conversations needing attention for the Quickchat AI Agent: $ARGUMENTS

If no Agent is named, resolve it with `list_scenarios` first. Surface flagged and
unresolved conversations, read the transcripts that matter, group the recurring
causes, and end with what to fix first.

Follow the `conversation-triage` skill.
