---
description: Investigate what customers are saying about one topic
argument-hint: "<topic> [agent name]"
---

Investigate this customer topic in Quickchat: $ARGUMENTS

Resolve the Agent with `list_scenarios` if it is not named. Use `get_topics` for
the intent split and `list_conversations` to find the conversations behind it,
then read enough transcripts to explain what customers actually mean — never page
through conversations to compute an aggregate.
