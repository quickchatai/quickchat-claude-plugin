---
description: Weekly performance review of a Quickchat AI Agent
argument-hint: "[agent name] [period, e.g. last 7 days]"
---

Run a performance review for the Quickchat AI Agent: $ARGUMENTS

If no Agent is named, call `list_scenarios` first — use it directly when there is
exactly one, otherwise ask which. Default the period to the last 7 days against
the 7 days before it.

Follow the `performance-review` skill.
